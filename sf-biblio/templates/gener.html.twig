{% extends "base.html.twig" %}

{% block title %}{{ parent() }}Listes Des Livres{% endblock %}

{% block main %}

    <section>
        <article>
            <h2>{{ genre.title }}</h2>
            {% for book in genre.books %}
                <h3>{{ book.title }}</h3>
            {% endfor %}
        </article>
    </section>

{% endblock %}